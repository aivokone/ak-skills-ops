## Seravo -> Local -> GitHub Private Import

For the end-to-end "clone from Seravo -> run locally in DDEV -> import DB via SSH
stream -> sync content with rsync -> push to GitHub private" workflow, use:

- `references/seravo-to-local-to-github.md`
